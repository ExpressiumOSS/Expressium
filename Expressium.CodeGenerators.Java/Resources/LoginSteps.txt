package BusinessTests.Steps;

import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;
import Bases.Asserts;

public class LoginSteps extends BaseSteps {

    @Given("I have logged in with valid user credentials")
    public void i_have_logged_in_with_valid_user_credentials() throws Exception {
       throw new io.cucumber.java.PendingException();
    }

    @Then("I should be redirected to the Home page")
    public void i_should_be_redirected_to_the_home_page() throws Exception {
       throw new io.cucumber.java.PendingException();
    }
}